import 'package:flutter/material.dart';

class FantasPage extends StatelessWidget {
  const FantasPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Fantasy'));
  }
}