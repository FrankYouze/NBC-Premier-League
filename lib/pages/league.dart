import 'package:flutter/material.dart';

class LeagueStand extends StatelessWidget {
  const LeagueStand({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Upd'));
  }
}